import path from "path"
export const rootPath = __dirname

console.log({rootPath})

export const PATH_DOWNLOADS = path.join('public', 'tmp')

export const EXTENSION_FILE = '.mp3'

export const FOLDER_NAME = 'tmp'